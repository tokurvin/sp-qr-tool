# ─────────────────────────────────────────────────────────────
# Local-only working files — never commit to the public repo.
# The public site = the HTML tools + 404.html only.
# See LOCAL_FOLDER_GUIDE.md for the full layout.
# ─────────────────────────────────────────────────────────────

# Claude Code context (must stay at repo root to be auto-loaded)
CLAUDE.md
.claude/

# Internal docs: README, CHANGELOG, spec, DFD
local-docs/

# QR sample/test images + capacity reference CSV
qr-assets/

# Local orientation guide for this folder
LOCAL_FOLDER_GUIDE.md

# Safety catch-alls for stray local assets at repo root
*.png
QR-code_v1-10_capacity.csv
dfd-eu-qr-v4.md
